{
  "description": "Content HTML utilities observed in local text blocks; gw-* tokens are custom CSS hooks, not Tailwind utilities. This list is reviewed, not a database export or build-time DB dependency.",
  "utilities": ["font-mono", "font-semibold", "line-through", "text-slate-500", "text-xs"]
}
